name: Deploy protected main

on:
  push:
    branches: [main]

permissions: {}

concurrency:
  group: aoe2scenario-production
  cancel-in-progress: false

jobs:
  deploy:
    if: >-
      github.event_name == 'push' &&
      github.repository == 'juliamauri/aoe2scenario' &&
      github.ref == 'refs/heads/main'
    runs-on: aoe2scenario-deploy
    steps:
      - name: Request fixed deployment
        shell: sh
        run: exec /usr/local/bin/aoe2scenario-deploy